import React, { memo, useMemo, useCallback } from 'react';
import { Component } from 'react'; 

class SpreadSheetComponent extends Component {
  render() {
    return (
      // <div ref={el => this.el = el} className="widget-box" style=:800,height:400></div>
      ""
    );
  }
}